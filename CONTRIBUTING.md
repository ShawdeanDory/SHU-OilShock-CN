# 协作规范

## 分支和 Pull Request

- `main` 始终保持可运行、可编译或至少结构完整。
- 每项任务从最新 `main` 创建独立分支，例如：
  - `data/eia-brent`
  - `model/problem1-forecast`
  - `model/problem2-macro`
  - `paper/methods`
- 一个 Pull Request 只解决一个明确问题，避免把数据、模型和论文大改混在一起。
- 合并前至少由一名队友检查来源、单位、日期范围和结果口径。

## 提交信息

建议采用简短前缀：

- `data:` 数据来源、下载或清洗
- `model:` 模型实现和检验
- `figure:` 图表
- `paper:` 论文内容
- `docs:` 说明和计划
- `fix:` 错误修复

示例：`data: add EIA Brent daily download script`

## 数据要求

- 仅使用免费公开数据。
- 每份数据必须记录来源 URL、下载日期、频率、单位、时间范围和许可条件。
- `data/raw/` 中的原始文件不得手工修改。
- `data/processed/` 必须能由 `code/` 中的脚本从原始数据重新生成。
- 如果数据文件过大或许可不允许再分发，只提交下载脚本、校验值和说明，不提交原文件。
- 禁止把 API 密钥、Cookie、个人路径或账号信息提交到仓库。

## 代码要求

- 所有脚本使用相对路径，不能写死个人电脑目录。
- 随机算法固定随机种子。
- 时间序列严格按时间划分训练、验证和测试区间。
- 处理脚本输出关键行数、缺失值、日期范围和单位检查结果。
- 模型结果统一写入 `results/`，论文不得引用终端中的临时数值。

## LaTeX 要求

- 主入口为 `paper/main.tex`。
- 章节文件放在 `paper/sections/`。
- 数据图和流程图统一从 `figures/` 引用。
- 合并前使用 XeLaTeX 连续编译两遍，检查交叉引用和中文字体。
- 不提交 LaTeX 辅助文件。

## Pull Request 检查

- [ ] 任务范围单一且说明清楚
- [ ] 数据来源和下载日期已记录
- [ ] 代码可从项目根目录运行
- [ ] 没有绝对路径、密钥和个人信息
- [ ] 结果与图表可以复现
- [ ] 对应的 `todo.md` 状态已更新
